import{_ as o,o as t,c as a,a2 as i}from"./chunks/framework.C2mit3tn.js";const E=JSON.parse('{"title":"架构约定","description":"","frontmatter":{},"headers":[],"relativePath":"zh/guide/architecture.md","filePath":"zh/guide/architecture.md","lastUpdated":1789196459000}'),s={name:"zh/guide/architecture.md"};function d(l,e,n,r,c,h){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="架构约定" tabindex="-1">架构约定 <a class="header-anchor" href="#架构约定" aria-label="Permalink to &quot;架构约定&quot;">​</a></h1><p>SweetEditor Compose 是<strong>声明式宿主</strong>：Composable 拥有 native session，Controller 只转发命令。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart LR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  host[宿主 UI] --&gt; controller[SweetEditorController]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  composable[SweetEditor] --&gt; session[RememberedEditorSession]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  composable --&gt; controller</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  session --&gt; native[Native EditorCore]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  controller --&gt; session</span></span></code></pre></div><h2 id="规则" tabindex="-1">规则 <a class="header-anchor" href="#规则" aria-label="Permalink to &quot;规则&quot;">​</a></h2><ul><li><code>SweetEditorController</code> 是宿主命令入口，<strong>不</strong>持有 <code>EditorCore</code> 或 native 文档句柄。</li><li><code>SweetEditor</code> 创建 <code>RememberedEditorSession</code> 并 <code>attach</code> Controller。</li><li>一个 Controller 只能绑一个编辑器。同一棵树重组没问题；第二个 <code>SweetEditor</code> 用同一实例会在 <code>attach</code> 失败。</li><li><code>whenReady { }</code>：已就绪则立刻跑，否则排队，在 attach 且 native 创建后刷新。</li><li>Ready 前变更 API 空操作。查询 API 返回空/默认（<code>getDocument()</code> 回退到 <code>initialText</code>）。</li><li>Controller 的 <code>dispose()</code> 只清事件监听和 ready 回调，<strong>不会</strong> <code>free_editor</code>。Native 生命周期跟 Composable 离开组合走。</li><li><code>getDocument()</code> 返回 <code>EditorDocument(text)</code> 文本快照，不是 native 句柄。</li></ul><h2 id="配置入口" tabindex="-1">配置入口 <a class="header-anchor" href="#配置入口" aria-label="Permalink to &quot;配置入口&quot;">​</a></h2><p><code>theme</code> / <code>settings</code> / <code>keyMap</code> 是 Composable 参数。Controller 另有 <code>applyTheme</code> / <code>setSettings</code> / <code>setKeyMap</code> 供就绪后命令式更新。</p><p>两边都写时，最后到达 session 的写入生效。每个字段只选一种写法。</p><h2 id="事件" tabindex="-1">事件 <a class="header-anchor" href="#事件" aria-label="Permalink to &quot;事件&quot;">​</a></h2><p>文档变更都进 session 唯一的 <code>dispatchActionResult</code>。查询 API 不返回 <code>EditorActionResult</code>。用 <code>controller.onTextChanged { }</code> 或 <code>controller.events.subscribe&lt;TextChangedEvent&gt; { }</code> 订阅。</p><h2 id="库不做的事" tabindex="-1">库不做的事 <a class="header-anchor" href="#库不做的事" aria-label="Permalink to &quot;库不做的事&quot;">​</a></h2><ul><li>没有内置 LSP / 语法引擎。Span 和补全由宿主 Provider 推入。</li><li>没有多光标（C API 无）。</li><li>Web 没有 <code>create_document_from_file</code>。</li></ul>`,12)])])}const u=o(s,[["render",d]]);export{E as __pageData,u as default};
