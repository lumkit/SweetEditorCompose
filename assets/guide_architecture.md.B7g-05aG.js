import{_ as t,o,c as a,a2 as s}from"./chunks/framework.C2mit3tn.js";const u=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"guide/architecture.md","filePath":"guide/architecture.md","lastUpdated":1789196459000}'),i={name:"guide/architecture.md"};function n(r,e,l,d,c,h){return o(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><p>SweetEditor Compose follows a <strong>declarative host</strong>. The composable owns the native session. The controller only forwards commands.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart LR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  host[Host UI] --&gt; controller[SweetEditorController]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  composable[SweetEditor] --&gt; session[RememberedEditorSession]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  composable --&gt; controller</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  session --&gt; native[Native EditorCore]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  controller --&gt; session</span></span></code></pre></div><h2 id="rules" tabindex="-1">Rules <a class="header-anchor" href="#rules" aria-label="Permalink to &quot;Rules&quot;">​</a></h2><ul><li><code>SweetEditorController</code> is the host command entry. It does <strong>not</strong> hold <code>EditorCore</code> or a native document handle.</li><li><code>SweetEditor</code> creates <code>RememberedEditorSession</code> and attaches the controller.</li><li>One controller may bind only one editor. Recomposition of the same tree is fine; a second <code>SweetEditor</code> with the same instance fails <code>attach</code>.</li><li><code>whenReady { }</code> runs immediately if already ready, otherwise it is queued and flushed after attach + native create.</li><li>Before ready, mutating APIs no-op. Query APIs return empty / default values (<code>getDocument()</code> falls back to <code>initialText</code>).</li><li><code>dispose()</code> on the controller clears event listeners and ready callbacks. It does <strong>not</strong> <code>free_editor</code>. Native lifetime follows the composable leaving composition.</li><li><code>getDocument()</code> returns <code>EditorDocument(text)</code> — a snapshot, not a native handle.</li></ul><h2 id="configuration-inputs" tabindex="-1">Configuration inputs <a class="header-anchor" href="#configuration-inputs" aria-label="Permalink to &quot;Configuration inputs&quot;">​</a></h2><p><code>theme</code>, <code>settings</code>, and <code>keyMap</code> are composable parameters. The controller also has <code>applyTheme</code> / <code>setSettings</code> / <code>setKeyMap</code> for imperative updates after ready.</p><p>If both are used, the last write that reaches the session wins. Prefer one style per field.</p><h2 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h2><p>Every document mutation goes through a single session <code>dispatchActionResult</code>. Query APIs do not return <code>EditorActionResult</code>. Subscribe with <code>controller.onTextChanged { }</code> or <code>controller.events.subscribe&lt;TextChangedEvent&gt; { }</code>.</p><h2 id="what-the-library-does-not-do" tabindex="-1">What the library does not do <a class="header-anchor" href="#what-the-library-does-not-do" aria-label="Permalink to &quot;What the library does not do&quot;">​</a></h2><ul><li>No built-in LSP or syntax engine. You push spans / completions via providers.</li><li>No multi-cursor (C API has none).</li><li>Web has no <code>create_document_from_file</code>.</li></ul>`,12)])])}const m=t(i,[["render",n]]);export{u as __pageData,m as default};
